# Implementation status

On a notched display the Dynamic Island now carries the floating controls and the hover rail stays hidden: the notch stays hoverable at all times (drawing nothing at rest), shows the live input level beside it during activity, the live transcript and Review while dictating, and the rail's dictate, meeting, assistant and settings row (pause and stop while recording) whenever the pointer is over it. Displays without a notch keep the rail unchanged, and the edge indicator is left with delivery results and microphone problems. **Chup! 1.0 (75)** is installed at `/Applications/Chup!.app` and verified running for 20 seconds; the offline native suite and the island presentation-policy check pass. Design asset: `Design/Previews/31-dynamic-island.png`.

Paste recovery now reports “Copied to clipboard” instead of the misleading “Couldn’t paste,” and Dictation diagnostics shows the trigger, route and safety reason for the last 20 delivery attempts. “Paste not confirmed” is reserved for a dispatched paste whose target did not verify the change.

The rail and dictation pill now share a 2-point physical screen-edge inset. Transient dictation errors and microphone/delivery messages clear after 1.5 seconds; saved audio and ongoing meeting status remain intact. Build, 104 core tests, the offline native suite and actual window placement/reset checks pass. See the newest validation entry for deployment status and logs.

The collapsed side rail now tracks from the physical screen edge, and dictation processing runs through an ordered background queue. A second recording can start while an earlier clip transcribes; saved audio and each captured insertion target remain independent. Build and native validation passed; see the latest `VALIDATION.md` entry for the manual long-processing check.

Screen-edge dictation controls now use a vertical waveform/spinner pill on right and left docks, with upright Cancel/Finish controls. Bottom docking stays horizontal; wider messages remain readable. Native panel dimensions and screen containment passed 54 transitions on the currently connected display. Animation preview: `Design/Previews/vertical-dictation.gif`.

Microphone startup now waits for delayed I/O notifications and can resume only the same input/format before reporting ready. Route checks account for a single-input Core Audio aggregate without confusing its numeric ID with a new microphone. Audio configuration notifications no longer synchronously wait on main during engine teardown. The signed C922 startup/recovery/cancellation tests pass; AirPods were unavailable for the final retest, so Bluetooth qualification remains open. See the latest entry in `VALIDATION.md`.

Dispatched text now collapses the dictation controls immediately without a “Text sent” popup or workspace banner. The internal dispatched-versus-verified distinction remains for diagnostics and clipboard recovery; actual delivery problems retain their messages. This applies to dictation and Paste Last and does not relax destination checks. The reported horizontal scrolling was not reproduced in the native sizing audit and the user reported it had cleared, so no layout changes were made for it.

The latest design pass replaces flat Settings content with grouped paper cards, inset status areas, concise introductions and expandable technical help. The same surfaces organize dictation history, notebook/personalization entries, meeting content, assistant exchanges and onboarding. Setup/editor sheets scroll with persistent action footers. All 14 Settings sections and 23 additional native layouts were visually reviewed before final installation; details and gallery are in `UI-LAYOUT-REVIEW.md`.

This is a substantial buildable native development preview, **not the full production feature set requested**. The remaining work stays in scope in PLAN.md. No AI response, speaker identity, hardware test or broadcast route is simulated as a successful integration.

Build 30 reliability correction: the build 29 unified log identified an AVFAudio hardware/client tap-format mismatch preceding the delayed Swift executor crash. Microphone restarts now recreate the engine, use the hardware input format and contain Objective-C audio exceptions before returning to Swift. The installed build passed three real meter-only startup/stop cycles on the selected C922 webcam at 16 kHz. Alert geometry now uses an explicit width and no competing frame animation; 36 real transitions on both attached displays passed. This supersedes the earlier assumption that removing individual executor checks resolved the crash. Long-running capture, USB unplug/replug and Bluetooth transitions still need qualification.

Latest reliability/local-model increment: the build 28 focus-observer crash exposed another native `MainActor.assumeIsolated` call. App-owned native callbacks now use explicit dispatch or a thread-safe synchronous focus generation counter. Focus invalidation retains its immediate behavior. New Settings panels provide durable, content-free stage timing and a working local transcription comparison lab. The lab runs Whisper Turbo and pinned Parakeet v3 sequentially on selected encrypted dictation audio, with per-model preparation/transcription times and explicit Copy; original history and paste targets are untouched. Both models passed synthetic English/Dutch checks with URL loading blocked. This does not establish representative accuracy or select a new default. Local VAD/diarization, generative cleanup and correction learning from the Ghost Pepper review remain follow-up work.

Shortcut crash fix: installed build 26 crash reports at 15:34 and 16:46 both terminate inside `MainActor.assumeIsolated` from the Quartz event callback. Quartz and Carbon shortcut callbacks now copy their payloads into an ordered main-queue bridge instead of asserting executor identity at the C callback boundary. A native regression fixture invokes the actual Quartz callback 1,000 times without posting system input and checks copying, order and invalidation. The twenty-second deployment launch check alone did not catch this event-driven failure.

Floating alert layout: clipboard and microphone messages wrap inside a 320 pt card with measured height, an immutable layout snapshot and explicit native width. Main-window notices wrap too. The real controller passed transitions on both attached displays; display hot-plug and full-screen Spaces remain manual checks.

Latest microphone iteration: automatic input resolution excludes a lid-blocked built-in microphone, follows default-device notifications and monitors alive/rate changes. Explicit input choices remain explicit. Dictation distinguishes quiet packets from missing packets, preserves stopped audio and rejects confirmed silent audio before cloud/local transcription. Audio settings now include a local, unsaved 30-second microphone test with real PCM frequency bars and signal feedback. Preview stops on navigation, sleep, device loss or starting another voice action. Physical microphone quality and device-change behavior still need the manual checks in VALIDATION.md.

## Implemented code and working local paths

- Xcode application and local ChupCore package, SwiftUI management window, menu-bar controls, native Settings with Command-comma, shared state, always-light paper workspace, independent dark glass rail and original Icon Composer icon.
- Independent nonactivating hover rail with a compact icon strip and separate click-through label overlays, hover delay, close delay/pointer corridor, drawers, docking/repositioning and display-change handling. Persistent hosting view, animated frame/content transitions, button feedback, Reduce Motion and Reduce Transparency support. Native UI rendering and sample-only design gallery with an interactive speaking/interruption motion study.
- Core Audio activity-based possible-meeting detection, four-second debounce, ten-second dismissal, per-call deduplication and two-minute snooze. Timeout never records.
- SQLCipher-encrypted SQLite WAL/FULL-synchronous metadata, FTS indexes, meeting notes with versions, idempotency receipts and guarded Undo. Metadata and transcript search indexes exist; Meetings UI currently searches title/people/tags, not transcript content.
- AES-GCM framed audio journal writer with bounded queue and rotation. Recovery of complete frames from truncated tails, authentication failure on tampering. One AVAudioEngine microphone owner with meeting/dictation consumers; process-tap app capture and explicit ScreenCaptureKit fallback adapters compile.
- Recording start/pause/resume/stop, separate source meters, recording legs and gaps, sleep pause, source/device-change handling. Continuous bounded-lookahead playback with seeking, shared-clock track mixing, preserved packet/leg gaps, pause/resume position and output-change handling. Native offline sample validation passed.
- Multiple independently editable/deletable bindings per action, including Fn plus modifier-only Control–Shift, stable saved IDs, per-binding conflicts/failures and a one-click external-keyboard alternative. Native shortcut registry with Carbon registration for ordinary chords and a separate Fn/mouse event path, persisted presets/rebinding, hold/release, Fn prefixes, repeat suppression, test mode, optional side mouse binding, known/internal conflict checks and permission feedback.
- Original AX destination capture and revalidation, secure-field exclusion, selection/value/focus checks, AX insertion, all-format clipboard snapshot and guarded clipboard restoration. No Return/send action.
- Dictation recording, recovery history, file transcription, cleanup/edit-selection backend, dictionary/snippets/styles settings, copy/retry/delete/paste-last. Raw transcript commits before a fallible cleanup call.
- Optional live-caption adapter and relay, resampling to 24 kHz, independent source sessions and capture-window provisional labels. Final file diarization in overlapping 90-second windows, atomic resumable jobs, conservative same-audio boundary/group reconciliation, correction/rename/split/merge overlays, and review of segmentation changes. Explicit encrypted voice enrollment, default-off cloud matching, suggested-match labels and deletion controls.
- Versioned live outlines and final summaries, pinned user interpretations, private thoughts, structured actions with guarded Undo, and source navigation. Quote/ID validation and independent backend evidence review; user notes are separate. Private text assistant with per-meeting saved conversations, bounded follow-up context, request cancellation and crash-interrupted state; visible proposed note edits, scoped local search/range/note/action tools and commit-before-acknowledgment.
- Trusted loopback development backend with separate app token, bounded input, no audio/text logs, verified model names, distinct Live and Realtime event protocols. Native GPT-Live-1 transport, addressed shared-owner input, acknowledged mute/unmute, silence clocking, client delegation, bounded context-revocable render queues, pinned headphone output and a rendered-PCM ledger. Voice UI is wired; bounded real-provider protocol checks passed in increment 8, while physical route qualification remains unperformed. See ITERATIONS-4-6.md.

- Iteration 7: Keychain-backed database encryption and verified plaintext/WAL migration; durable deletion plans/tombstones, opt-in retention, private-aware JSON exports, streamed password backups and isolated restore; encrypted native/backend request recovery and provider usage snapshots. Feature-scoped onboarding, OS Privacy links/status, launch at login and the Chup! product name. See [the report](ITERATION-7.md).

## Genuine external qualification blockers

- Increment 8 used an existing trusted-host environment credential for seven real-provider synthetic smoke checks: account model access, English/Dutch file transcription, file diarization, evidence summary/cached replay, GPT-Live-1 startup/mute/close and live transcription. All passed after fixing action metadata validation. Representative accuracy, full conversation/delegation/interruption, latency and invoice reconciliation remain unqualified.
- No signed capture session with granted Microphone/Input Monitoring/Accessibility/System Audio permissions was run. Device routing and conferencing behavior cannot be certified from compilation or pure tests.
- The separate Chup! Mic prototype builds but is not installed or bundled. Native mic-plus-assistant mixing, remote exclusion and isolated assistant recording are implemented and tested offline. Licensing/signing/installation, hardware privacy/broadcast qualification and speaker AEC remain open. A separately named participant needs a platform/bot integration.
- Apple Development installation and Developer ID Release/DMG signing now work. No notarization profile was supplied to packaging, and no Apple upload/review was attempted. Notarization, update feed and App Store distribution remain open.

## Remaining engineering (not just testing)

P0/P1: production qualification of addressed Live input/output, narration fidelity and routing privacy; licensed/signed driver installation lifecycle; speaker AEC; account-level billing reconciliation and mixed-modality pricing; semantic reconciliation for materially different overlap transcriptions; qualified stable acoustic speaker identities; physical VoiceOver/keyboard qualification of the new explicit rail focus path; physical continuous playback; large-workspace migration and backup fault qualification; representative provider voice/usage qualification; broader hardware recovery and source qualification.

Privacy gate: headphones alone never prove private input. Mode switching recreates the Live context. The native outgoing graph is implemented; Core Audio checks exclusive call-device input and other input readers. These 100 ms checks are not atomic against another app switching inputs, so the route remains a development prototype pending qualification. Text fallback is available.

AudioOwner now converts PCM into preallocated bounded slots; remote adapters still allocate AVAudioPCMBuffer objects and use dispatch; hard realtime performance and two-hour soak remain release gates. Microphone source loss pauses capture; remote failure leaves microphone capture running, marks a remote gap, and offers explicit same-process reconnect. Sample-rate discontinuities rotate files. Cross-device drift and acoustic echo cancellation are not qualified.

Optional later scope: calendar integration, platform bots, cloud sync, wake phrases, open conversation and wider language evaluation. Nothing in this section is represented as active behavior in the UI.

See [the playback and assistant increment](PLAYBACK-AND-ASSISTANT.md) for implementation details, test evidence and remaining limits.

See [iterations 1–3](ITERATIONS-1-3.md) for the latest capture, dictation and transcript implementation, precise limits, and **32 Swift + 12 backend tests** plus native offline validation.

Latest: [iteration 7](ITERATION-7.md), **50 Swift + 20 backend tests**, native build/offline audio and localhost Live transport/orchestration checks passed. The renamed driver built without installation. **One planned product increment remains: 8 — qualification and distribution.** Signed onboarding/login registration, real provider accounting and physical audio/privacy gates remain unperformed.

Local delivery update (September 15): **Chup! 1.0 (3)** is installed at `/Applications/Chup!.app`. The signed installed bundle passed strict code-signature verification and offline native audio/localhost Live checks. The deployment script increments future build numbers, retains the previous installed app, and is required after every implementation iteration by AGENTS.md. This does not qualify real permissions, capture or provider access.

Latest delivery: **Chup! 1.0 (4)** is installed in `/Applications` with multiple shortcuts per action. **58 Swift tests** and native installed-app shortcut/persistence, offline audio and localhost Live checks passed. The previous installed version was archived for rollback. Physical external-keyboard and global event-order qualification remains manual; see the latest section in VALIDATION.md.

## Increment 8 independent work — September 15

See [PHASE-8.md](PHASE-8.md) for delivered engineering and evidence, and [MANUAL-TESTS.md](MANUAL-TESTS.md) for acceptance steps. Full aligned WAV export, encrypted waveform indexes, transcript/note meeting search, explicit keyboard rail access, allowlisted diagnostics, storage/usage hardening and Developer ID packaging are implemented. **65 Swift and 22 backend tests passed**, along with native offline checks and seven real-provider synthetic checks. An accelerated two-hour export passed; no two-hour hardware recording was performed. Increment 8 remains open for the documented hardware, privacy and public-release gates.

Installed delivery for this slice: **Chup! 1.0 (5)** at `/Applications/Chup!.app`, locally signed and verified on the installed copy. The previous build is retained for rollback.

Permission UI delivery completed: **Chup! 1.0 (7)** installed at `/Applications/Chup!.app`. The user explicitly authorized force-quitting the running app for this replacement. Strict signature verification and installed native fixtures passed; the updated app was reopened. Log: `.artifacts/permissions-deploy-final.log`. This supersedes the earlier build 6 deployment deferral.

Permission UI cleanup: removed the expanded missing-app help and Finder button. **Chup! 1.0 (8)** built, signed, installed at `/Applications/Chup!.app`, and restarted. Installed native checks and strict signature verification passed. Log: `.artifacts/permission-cleanup-deploy.log`.

Compact permission links delivered: **Chup! 1.0 (10)** installed and restarted at `/Applications/Chup!.app` after the user accepted the Xcode license. Each permission title now has a small arrow to its Privacy pane; the text button, breadcrumb and empty action row are removed. Native build, installed fixtures and strict signature verification passed. Log: `.artifacts/permission-arrow-deploy-final.log`.

Permission refresh control: replaced Check access again text with a 40 pt circular button, native activity animation and a confirmed-success checkmark. Unresolved permissions use a distinct icon and accessible description; permission changes invalidate success immediately. Checks cancel when the view disappears and cannot overlap. **Chup! 1.0 (11)** installed and restarted. Native build, installed offline checks and strict signature verification passed. Actual permission and Reduce Motion interaction remain manual checks. Log: `.artifacts/permission-refresh-deploy.log`.

Permission refresh correction: restored the visible Check access again label beside the animated icon. Explicit result states distinguish enabled (checkmark), missing/denied access (exclamation), capture-only verification pending (clock) and no selection (refresh). Unknown capture access no longer looks like a failure; it is not falsely claimed as granted. **69 Swift tests passed**, including four new result-state tests; installed native checks and signature verification passed. **Chup! 1.0 (13)** installed and restarted. Logs: `.artifacts/permission-refresh-core.log`, `.artifacts/permission-refresh-result-deploy.log`.

## Dictation recovery and indicator — build 14

**Chup! 1.0 (14)** is installed at `/Applications/Chup!.app`, verified and restarted. Saved dictations now support local playback and seeking even when transcription failed. Starting capture or a voice session stops dictation playback. A dedicated nonactivating right-edge indicator shows microphone startup, live listening levels and processing independently of the hover rail preference.

New clips up to two seconds are checked locally for silence or strong non-speech evidence before cloud processing. Ignored clips remain recoverable under Show ignored clips; uncertain clips and short spoken words are retained. This is a conservative classifier, not qualified speech recognition. The preset is now Default preset; competitor references were removed from application source and documentation.

**72 Swift tests**, native installed-app fixtures and additional synthetic noise/English “No”/Dutch “Nee” checks passed. Physical playback, global shortcut interaction across Spaces and representative noise accuracy still need manual checks. See [local transcription research](LOCAL-TRANSCRIPTION.md): WhisperKit/Turbo is recommended for the first English/Dutch offline integration, but no offline ASR model/runtime has been installed yet.

Crash follow-up: **Chup! 1.0 (15)** is installed at `/Applications/Chup!.app` with explicit MainActor delivery for the shortcut, workspace and meeting-detection timers. Build, native timer regression and signed installed-app fixtures passed. The observed build 14 crash was inside the shortcut timer’s executor-isolation check; extended runtime qualification remains open.

Button label update: dictation's idle button now reads **test**. Chup! 1.0 (16) installed at `/Applications/Chup!.app` and reopened. Native build, strict code-signature verification and installed fixtures passed. Log: `.artifacts/test-button-deploy.log`.

## Offline dictation delivery — build 17

**Chup! 1.0 (17)** is installed at `/Applications/Chup!.app`. Offline dictation is now implemented, superseding the earlier research-only status. The pinned WhisperKit/Turbo runtime and verified model/tokenizer are installed on this Mac. Settings → Dictation exposes routing, readiness, download/cancel/retry and removal. Auto falls back after cloud failure, missing configuration or disabled cloud processing; Local only never calls the cloud. History records provider and unapplied-cleanup details. Existing saved recordings can be transcribed with Retry audio.

**75 core tests**, native fixtures and real synthetic English/Dutch local inference checks passed. The latter exercised encrypted journals, committed text, network-blocked fresh initialization, local-only processing, cloud-off behavior, one cloud-failure fallback, cancellation and selected-text command protection. No real user audio was processed. Local dictation does not add local meeting diarization, summaries, AI editing or conversational voice. See VALIDATION.md for logs and remaining physical qualification.

Dictation delivery update: **Chup! 1.0 (19)** installed at `/Applications/Chup!.app` and reopened. Circular processing is visible in the compact rail, expanded rail, floating indicator and workspace sidebar. Completion verifies the original destination; missing/failed insertion copies the transcript and shows brief nonactivating recovery feedback. Newer clipboard actions remain protected. 78 core tests and installed native fixtures passed; real cross-application paste remains a manual acceptance check.

Live waveform delivery: **Chup! 1.0 (20)** installed at `/Applications/Chup!.app`, signed and verified, with installed native fixtures passing. The app was reopened. Live microphone history now animates all dictation listening surfaces; 81 core tests passed. Physical speech-to-screen behavior remains a manual check.

Generic insertion fix: **Chup! 1.0 (21)** installed at `/Applications/Chup!.app`, signed, verified and restarted. Capability-based routing supports guarded paste into focused text controls without writable caret metadata; no application-name whitelist is used. Input/focus changes and secure input block delivery, uncertain results retain recovery text, and opaque controls receive single-line text only. **85 core tests** and native installed-app fixtures passed. Live cross-application dictation remains a manual acceptance check. Logs: `.artifacts/focused-paste-core.log`, `.artifacts/focused-paste-native.log`, `.artifacts/focused-paste-deploy.log`.

Compact controls delivery: **Chup! 1.0 (22)** installed at `/Applications/Chup!.app`, signed, verified and restarted. The idle handle is 10 × 36 pt; dictation replaces it with a 148 × 36 pt dark glass pill containing Cancel, eleven live waveform bars and Finish. Processing crossfades to a circular indicator. Meeting/assistant status remains visible separately. Native checks passed before signing and against the installed bundle. `Design/Previews/22-compact-motion.gif` shows native animation with labeled illustrative levels. Physical microphone, Spaces, docking and accessibility qualification remain manual. Log: `.artifacts/compact-pill-deploy.log`.

Unified popups, live frequency EQ and native Paste: **Chup! 1.0 (24)** installed at `/Applications/Chup!.app`, signed, verified and restarted. Hover controls, label overlays, drawers and custom notification cards share compact charcoal glass and cream controls. The EQ uses real PCM frequency bands with additional headroom. Generic clipboard delivery invokes an enabled ordinary Paste command independently of its shortcut, with Command-V fallback. **88 core tests** and installed native fixtures passed. A real desktop test against the signed installed app delivered exactly one synthetic line through Native Paste into a uniquely marked isolated terminal, confirmed receipt, and posted no Return. Physical dictation in the user's actual editor and microphone-to-screen behavior remain manual acceptance checks. Preview: `Design/Previews/23-unified-popups.png`. Logs: `.artifacts/popup-paste-deploy.log`, `.artifacts/paste-desktop-installed.log`.

Deployment identity: **Chup! 1.0 (25)** installed and launched at `/Applications/Chup!.app`. The installer now compares executable UUIDs and records a stable exact-path process after 20 seconds. Settings → General shows the startup-captured version/build and bundle path; the menu-bar menu shows version/build. No stale installed copy was found during the investigation: build 24 matched its build output, but no Chup! process was running initially and no new crash report explained that absence. Build 25 native/signature checks passed. A live pointer-only hover check observed the new rail expanding from 10 × 36 pt to 44 × 181 pt (native content fitting reduces the requested 188 pt height). No microphone or paste was triggered. Log: `.artifacts/build-identity-deploy.log`.
